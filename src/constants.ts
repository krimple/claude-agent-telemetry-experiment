/**
 * Telemetry configuration constants
 */
export const TELEMETRY_CONFIG = {
  MIN_METRIC_INTERVAL: 100,
  MAX_METRIC_INTERVAL: 3600000,
  DEFAULT_METRIC_INTERVAL: 10000,
  DEFAULT_SERVICE_NAME: "claude-sub-agent",
} as const;
